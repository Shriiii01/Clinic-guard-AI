services:
  clinicguard:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./server:/app
      - ./models/llama-3-8b:/app/models/llama-3-8b
    networks:
      - ai-network

networks:
  ai-network:
    driver: bridge